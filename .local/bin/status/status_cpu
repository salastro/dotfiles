#!/bin/sh 

cpu=$(top -bn1 | grep "Cpu(s)" | sed "s/.*, *\([0-9.]*\)%* id.*/\1/" | awk '{print 100 - $1}')

# isGreater=$(echo "$cpu > 10" | bc)
# [ "$isGreater" -eq "1" ] && cpu="^c#ff0000^$cpu^d^"

# printf "%.*f%%" 1 $cpu 2> /dev/null
echo "$cpu%"
 
#printf "%.*f%%" 0 $(mpstat 1 1 | awk 'END{print 100-$NF"%"}')
